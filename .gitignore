themes/indiebrain/resources/css/.sass-cache/*
/resume/resume*.pdf
/resume/resume*.tex

# Hugo migration: Org is the source of truth. content/ is generated by
# ox-hugo and public/ by Hugo; both are built in CI, never committed.
/content/
/public/
/resources/_gen/
/.hugo_build.lock
# Project-local Emacs packages bootstrapped by build/export.el.
/.elpa/
/static/ox-hugo/
/static/resume/
